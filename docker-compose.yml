services:
    # ─── Express Backend ───────────────────────────────
    backend:
        build:
            context: .
            target: production
        container_name: taskmaster-backend
        ports:
            - "3001:3001"
        env_file:
            - .env
        environment:
            NODE_ENV: production # overrides .env.docker for clarity

